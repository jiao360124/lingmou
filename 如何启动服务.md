# 🚀 服务启动指南

## 问题已识别

- ❌ Gateway 未运行（约 3.5 小时）
- ❌ Dashboard 未运行（依赖未安装）
- ❌ Agent 未运行（随 Gateway 停止）

---

## 解决方案

### 方案 1：使用一键启动脚本（推荐）

**手动操作步骤**：

1. **打开 PowerShell 或 CMD**

2. **导航到工作目录**
   ```bash
   cd C:\Users\Administrator\.openclaw\workspace
   ```

3. **运行一键启动脚本**
   ```bash
   start-openclaw.bat
   ```

**脚本功能**：
- ✅ 自动检查所有文件是否存在
- ✅ 自动安装缺失的依赖
- ✅ 自动启动 Gateway
- ✅ 自动启动 Dashboard

---

### 方案 2：分步手动启动

#### 步骤 1：安装 Dashboard 依赖

```bash
cd C:\Users\Administrator\.openclaw\workspace
npm install express socket.io --legacy-peer-deps
```

#### 步骤 2：启动 Gateway

```bash
openclaw gateway start
```

#### 步骤 3：启动 Dashboard

在新的终端窗口：
```bash
cd C:\Users\Administrator\.openclaw\workspace
node dashboard-server.js
```

---

## 验证服务状态

### 1. 检查 Gateway
```bash
openclaw gateway status
```
应该看到：`✅ Gateway 已启动`

### 2. 检查 Dashboard
```bash
netstat -ano | findstr :3000
```
应该看到端口 3000 正在监听

### 3. 访问 Dashboard
打开浏览器访问：http://localhost:3000

---

## 文件清单

### ✅ 已创建的文件

1. **dashboard-server.js** (8.9KB)
   - Express.js 服务器
   - WebSocket 实时推送
   - 4 个 REST API 端点

2. **dashboard.html** (34.9KB)
   - 响应式 UI
   - 3D 动态背景
   - 玻璃拟态设计
   - AI 智能洞察

3. **public/chart.js** (6.0KB)
   - 简易图表库
   - 支持：折线图、柱状图、饼图

4. **package.json** (0.4KB)
   - 依赖配置
   - 启动脚本

5. **start-openclaw.bat** (0.8KB)
   - 一键启动脚本

---

## 创新特性

### Dashboard 独有功能
- ✨ **3D 动态背景** - 粒子系统动画
- 🌈 **玻璃拟态 UI** - 毛玻璃效果
- 🤖 **AI 智能洞察** - 自动生成优化建议
- 📊 **4个实时图表**:
  - 成本趋势（折线图）
  - 模型性能（柱状图）
  - Fallback 使用（饼图）
  - 延迟监控（折线图）
- 💬 **WebSocket 推送** - 2秒实时更新
- 🎨 **主题切换** - 亮色/暗色模式
- 📱 **响应式设计** - 支持手机、平板、桌面

---

## 常见问题

### Q: npm install 失败怎么办？
**A**: 尝试使用 `--legacy-peer-deps` 参数，或使用国内镜像：
```bash
npm config set registry https://registry.npmmirror.com
npm install express socket.io --legacy-peer-deps
```

### Q: Gateway 启动失败？
**A**: 检查端口 18789 是否被占用：
```bash
netstat -ano | findstr :18789
```

### Q: Dashboard 无法访问？
**A**:
1. 确认端口 3000 正在监听
2. 检查防火墙设置
3. 尝试清除浏览器缓存

---

## 下一步

✅ 服务启动后：

1. 访问 http://localhost:3000 查看创新 Dashboard
2. 配置 AI 智能洞察参数
3. 调整图表显示设置
4. 探索 3D 背景和主题切换功能

---

**创建时间**: 2026-02-17 17:09
**状态**: 等待主人手动执行启动脚本
