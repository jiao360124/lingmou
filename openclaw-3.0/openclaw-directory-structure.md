# 📁 OpenClaw 目录结构详解

## 🎯 总体概览

```
C:\Users\Administrator\.openclaw\
│
├── workspace\                          ← 主工作区（包含所有项目）
│   └── openclaw-3.0\                   ← OpenClaw 3.0 项目
│       ├── package.json                ← 项目配置
│       ├── node_modules\               ← 项目依赖
│       ├── index.js                    ← 主入口文件
│       ├── doctor.js                   ← Doctor 诊断工具
│       └── ...
│
├── AppData\Roaming\npm\                ← npm 全局安装目录
│   └── node_modules\
│       └── openclaw\                   ← OpenClaw 全局安装
│           └── index.js
│
└── Program Files\nodejs\               ← Node.js 安装目录
    ├── node.exe                        ← Node.js 可执行文件
    ├── npm.cmd                         ← npm 命令
    └── ...
```

---

## 📂 详细目录结构

### 1. **workspace/** - 主工作区

这是 OpenClaw 的主工作目录，包含所有项目。

```
C:\Users\Administrator\.openclaw\workspace\
│
├── .openclaw\                          ← 工作区配置和状态
│   ├── .openclaw\                      ← 工作区子目录
│   │   ├── logs\                       ← 日志目录
│   │   │   ├── 2026-02-16.log         ← 每日日志
│   │   │   └── ...
│   │   ├── memory\                     ← 长期记忆
│   │   │   ├── 2026-02-15.md
│   │   │   ├── 2026-02-16.md
│   │   │   └── ...
│   │   ├── data\                       ← 数据存储
│   │   ├── cleanup-guide.md            ← 清理指南
│   │   ├── cleanup-report.md           ← 清理报告
│   │   ├── CLEANUP-SUMMARY.md          ← 清理总结
│   │   └── ...
│   ├── .openclaw.backup\               ← 备份目录
│   ├── openclaw.json.bak.1             ← 备份文件
│   ├── openclaw.json.bak.2             ← 备份文件
│   ├── openclaw.json.bak.3             ← 备份文件
│   ├── openclaw.json.bak.4             ← 备份文件
│   └── cleanup.bat                     ← Windows 清理脚本
│
├── openclaw-3.0\                       ← OpenClaw 3.0 项目
│   ├── package.json                    ← 项目配置
│   ├── package-lock.json               ← 锁文件
│   ├── node_modules\                   ← 项目依赖（下载的包）
│   ├── config\                         ← 配置目录
│   │   ├── index.js                    ← 主配置
│   │   ├── gateway.config.js           ← Gateway 配置
│   │   ├── dashboard.config.js         ← Dashboard 配置
│   │   ├── report.config.js            ← Report 配置
│   │   └── cron.config.js              ← Cron 配置
│   ├── utils\                          ← 工具函数
│   │   ├── logger.js                   ← 日志系统
│   │   ├── error-handler.js            ← 错误处理
│   │   ├── retry.js                    ← 重试机制
│   │   └── cache.js                    ← 缓存系统
│   ├── dashboard\                      ← Dashboard 模块
│   │   ├── server.js                   ← 服务器
│   │   └── index.html                  ← 前端页面
│   ├── report-sender.js                ← Report 发送器
│   ├── cron-scheduler\                 ← Cron 调度器
│   │   └── index.js                    ← 调度器主文件
│   ├── services\                       ← 服务层
│   │   └── data-fetcher.js             ← 数据获取
│   ├── test\                           ← 测试
│   │   ├── runner.js                   ← 测试运行器
│   │   └── unit\                       ← 单元测试
│   ├── PHASE2-FINAL-SUMMARY.md         ← Phase 2 总结
│   ├── doctor.js                       ← Doctor 诊断工具
│   └── index.js                        ← 主入口文件
│
└── [其他项目]                           ← 可能的其他项目
```

---

## 🎯 核心目录说明

### 📂 **workspace/.openclaw/** - 工作区配置

**用途**:
- ✅ 保存 OpenClaw 的配置和状态
- ✅ 存储长期记忆（memory 目录）
- ✅ 存储日志（logs 目录）
- ✅ 存储数据（data 目录）
- ✅ 保存清理脚本和报告

**文件说明**:
- `.openclaw.json` - 工作区配置文件
- `memory/YYYY-MM-DD.md` - 每日的记忆文件
- `logs/*.log` - 日志文件
- `cleanup-*.md` - 清理相关文档
- `cleanup.bat` - Windows 清理脚本

---

### 📂 **workspace/openclaw-3.0/** - OpenClaw 3.0 项目

**这是主要的项目目录！**

**主要文件**:

| 文件 | 用途 |
|------|------|
| `package.json` | 项目配置，定义依赖和脚本 |
| `package-lock.json` | 依赖版本锁定文件 |
| `index.js` | 项目主入口文件 |
| `doctor.js` | Doctor 诊断工具 |
| `node_modules/` | 项目依赖包（自动下载） |

**目录结构**:

| 目录 | 用途 |
|------|------|
| `config/` | 配置文件目录 |
| `utils/` | 工具函数目录 |
| `dashboard/` | Dashboard 模块 |
| `report-sender.js` | Report 发送器 |
| `cron-scheduler/` | Cron 调度器 |
| `services/` | 服务层 |
| `test/` | 测试文件 |

---

### 📂 **AppData/Roaming/npm/** - npm 全局安装目录

**用途**:
- ✅ 安装全局 npm 包（如 openclaw）
- ✅ 保存全局配置
- ✅ 存储 npm 全局模块

**文件说明**:
- `node_modules/openclaw/index.js` - OpenClaw 全局安装

**常用全局包**:
- `npm install -g <package>` - 安装全局包
- `npm list -g` - 列出全局安装的包

---

### 📂 **Program Files/nodejs/** - Node.js 安装目录

**用途**:
- ✅ 存储 Node.js 可执行文件
- ✅ 存储 npm 命令
- ✅ 存储全局工具

**关键文件**:
- `node.exe` - Node.js 可执行文件
- `npm.cmd` - npm 命令脚本
- `npx.cmd` - npx 命令脚本
- `nvm.cmd` - nvm 版本管理器（如果安装）

---

## 🔗 重要文件和目录的关系

### 1. **工作区与项目的关系统**

```
workspace/openclaw-3.0/          ← 项目目录
    ├── package.json             ← 定义需要哪些依赖
    ├── node_modules/            ← 下载依赖到这个目录
    └── index.js                 ← 项目入口文件
```

**运行方式**:
```powershell
cd workspace/openclaw-3.0  # 进入项目目录
node index.js               # 运行项目
npm install                 # 安装依赖
```

---

### 2. **Node.js 安装与项目的关系统**

```
Program Files/nodejs/node.exe  ← Node.js 可执行文件（全局）
    │
    └── npm (npm.cmd)           ← npm 命令（全局）
        │
        └── npm install -g openclaw  ← 全局安装 OpenClaw
            │
            └── AppData/Roaming/npm/node_modules/openclaw/  ← 全局安装位置
```

**运行方式**:
```powershell
node --version                # 使用全局 Node.js
npm --version                 # 使用全局 npm
openclaw --version            # 使用全局 OpenClaw
```

---

### 3. **全局安装与项目使用的关系统**

```
全局安装 (OpenClaw)           ← 作为一个独立程序安装
    ↓
AppData/Roaming/npm/node_modules/openclaw/
    ↓
    ├── index.js              ← 主文件
    └── config/                ← 配置文件
```

**使用方式**:
```powershell
openclaw gateway start        # 启动 Gateway（使用全局安装）
openclaw doctor               # 运行诊断（使用全局安装）
```

---

## 🎯 为什么需要在项目目录下工作？

### **因为项目依赖配置！**

```
workspace/openclaw-3.0/
    ├── package.json           ← 依赖配置
    └── node_modules/          ← 依赖安装位置
        └── express/           ← 项目需要的依赖
            └── index.js       ← 依赖文件
```

**必须的原因**:

1. **读取 package.json**
   - 脚本需要从当前目录读取 `package.json`
   - 了解需要安装哪些依赖

2. **安装依赖**
   - `npm install` 会安装到当前目录的 `node_modules/`
   - 项目依赖只在这个目录有效

3. **运行脚本**
   - 脚本会从当前目录查找配置和依赖
   - 需要在项目根目录运行

---

## 💡 工作流程说明

### **正常的工作流程**:

```
1. 进入项目目录
   cd C:\Users\Administrator\.openclaw\workspace\openclaw-3.0

2. 安装依赖
   npm install

3. 运行项目
   node index.js

4. 使用全局命令
   openclaw doctor
   openclaw gateway start
```

---

### **错误的工作流程**:

```
❌ 在错误的目录运行
   C:\Users\Administrator\.openclaw\
   node index.js          ← 找不到 index.js！

❌ 在错误的目录安装依赖
   C:\Users\Administrator\.openclaw\
   npm install            ← 安装到错误的位置！

❌ 忘记进入项目目录
   node index.js          ← 找不到 package.json！
```

---

## 📊 目录大小估算

| 目录 | 预估大小 | 说明 |
|------|---------|------|
| `workspace/.openclaw/` | ~10-50 MB | 配置、记忆、日志 |
| `workspace/openclaw-3.0/` | ~100-500 MB | 项目代码、依赖 |
| `AppData/Roaming/npm/` | ~100-300 MB | 全局 npm 模块 |
| `Program Files/nodejs/` | ~50 MB | Node.js 本体 |
| **总计** | **~260-900 MB** | - |

---

## 🔧 维护建议

### **定期清理**:

1. **清理日志**
   ```
   workspace/.openclaw/logs/
   清理旧的 .log 文件
   ```

2. **清理缓存**
   ```
   workspace/openclaw-3.0/.cache/
   清理缓存文件
   ```

3. **清理备份**
   ```
   workspace/.openclaw/*.bak*
   清理旧的备份文件
   ```

---

### **定期备份**:

1. **备份工作区**
   ```
   备份整个 workspace 目录
   ```

2. **备份配置**
   ```
   备份 workspace/.openclaw/ 下的配置文件
   ```

---

## ✅ 总结

### **核心要点**:

1. **Node.js 安装位置**: `C:\Program Files\nodejs\`（全局）
2. **项目目录**: `C:\Users\Administrator\.openclaw\workspace\openclaw-3.0\`（项目）
3. **npm 全局目录**: `C:\Users\Administrator\AppData\Roaming\npm\`（全局包）
4. **工作区配置**: `C:\Users\Administrator\.openclaw\workspace\.openclaw\`（配置）

### **不要混淆**:

- ❌ 不要在项目目录安装 Node.js
- ❌ 不要在全局目录编写项目代码
- ✅ 在项目目录运行项目命令
- ✅ 使用全局命令管理 Node.js 和包

---

**创建时间**: 2026-02-17 02:50
**版本**: 1.0
